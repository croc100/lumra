version: 2

project_name: lumra

before:
  hooks:
    - go mod tidy

builds:
  - id: lumra
    main: ./cmd/lumra
    binary: lumra
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: ["zip"]
    files:
      - README.md
      - ROADMAP.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^chore(deps)"
      - "^ci:"
      - "^docs:"
  groups:
    - title: "New Features"
      regexp: "^feat"
      order: 0
    - title: "Bug Fixes"
      regexp: "^fix"
      order: 1
    - title: "Other"
      order: 999

# Homebrew distribution as a Cask (goreleaser deprecated `brews`/formulae in
# favour of casks for pre-compiled binaries). Installs as
# `brew install croc100/tap/lumra`.
homebrew_casks:
  - name: lumra
    binaries:
      - lumra
    homepage: "https://lumra.crode.net"
    description: "Diagnose why a site is blocked, throttled, or tampered with — and where the interference originates"
    directory: Casks
    repository:
      owner: croc100
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    # Unsigned binary: strip the quarantine xattr so macOS doesn't flag the
    # cask as "damaged and cannot be opened" on first run.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/lumra"]
          end

scoops:
  - name: lumra
    repository:
      owner: croc100
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: bucket
    homepage: "https://lumra.crode.net"
    description: "Diagnose why a site is blocked, throttled, or tampered with — and where the interference originates"
    license: "Elastic-2.0"

release:
  github:
    owner: croc100
    name: lumra
  name_template: "v{{ .Version }}"
  footer: |
    ## Install

    **macOS / Linux (Homebrew)**
    ```sh
    brew install croc100/tap/lumra
    ```

    **Go install**
    ```sh
    go install github.com/croc100/lumra/cmd/lumra@latest
    ```

    **Direct download**
    Grab the binary for your platform from the assets below.

    > Deep RST/TTL attribution needs a raw socket: run elevated
    > (`sudo` / `cap_net_raw`). Every other signal works unprivileged.

    ---
    **Site**: https://lumra.crode.net
    **Roadmap**: https://github.com/croc100/lumra/blob/main/ROADMAP.md
